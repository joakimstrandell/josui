{
  "name": "josui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run -r build",
    "dev": "vp run -r dev",
    "lint": "vp run -r lint",
    "check": "vp check",
    "format": "vp fmt --write",
    "knip": "knip",
    "test": "vp run -r test",
    "clean": "vp run -r clean && rm -rf node_modules",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "vp run --filter './packages/*' build && changeset publish",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@types/node": "^20.0.0",
    "knip": "^5.82.1",
    "typescript": "^5.7.0",
    "vite-plus": "^0.1.14"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0"
}
